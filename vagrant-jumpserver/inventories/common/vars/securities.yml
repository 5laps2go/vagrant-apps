github_public_key: "{{ lookup('pipe', 'fgrep github ~/.ssh/authorized_keys') }}"

selinux_booleans:
   - { name: 'httpd_can_network_connect', state: 'yes', persistent: 'yes' }
firewall:
   - { service: 'https', state: 'enabled' }


